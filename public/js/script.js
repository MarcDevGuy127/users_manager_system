document.addEventListener('DOMContentLoaded', () => {
    console.log('Script JavaScript carregado!');

    // Exemplo: Confirmação de exclusão (já tratada no HTML com onclick)
    // Para uma versão mais avançada, faríamos AJAX aqui.
});
